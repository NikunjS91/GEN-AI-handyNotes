today we will work to build a project which will not just interact with database but also it will perform operation on databases.

for that we must need all api keys like we groq and open_ai
also we need langchain and langgraph.
then we need the database, for that we have a tool called SQLdatabase tool kit in langchain-community library

so the SQLDatabase is used to define as well as create the tables, inside the database  this gives you a variable with the agent cant communiate with database he can run queries.
SQLDatabaseToolKit is a tool which connect the agent with the SQLDatabase object.
 then we can get toolkit with this SQLDatabaseToolkit(db=db,llm=model) db is the db object and model is the langchain llm we called
 then to give agent access to tools we need to get tools object too which can be done with this : tools = toolkit.get_tools()

 then comes the main part which is system prompt this tells the agent what se should do with referances see this below and refer : 
 system_prompt = """
You are a task management assistant that interacts with a SQL database containing a 'tasks' table

TASK RULES:
1. Limit SELECT queries to 10 results max with ORDER BY created_at DESC
2. After CREATE/UPDATE/DELETE, confirm with SELECT query
3. If the user requests a list of tasks, present the output in a structured table format to encsure a clean and ogranized display in the browser."

CRUD OPERATIONS:
    CREATE: INSERT INTO tasks(title, description, status)
    READ: SELECT * FROM tasks WHERE ... LIMIT 10
    UPDATE: UPDATE tasks SET status=? WHERE id=? OR title=?
    DELETE: DELETE FROM tasks WHERE id=? OR title=?

Table schema: id, title, description, status(pending/in_progress/completed), created_at.
"""

till now we havent defined the agent yet.

so now we use a function to define agent with same model,checkpointer, tools,system_prompt,
    we have predefined these variable we are just passing it in the call agent
    why we made function becasue everytime we prompt on streamlit it refreshed and memeory's thread_id is changed so we used @st.cache_resource which keep the data in cache and use it again when the refresh is done.
    the refresh is basically a re-render.
    
then everything is same we generating the response and all.
but the main part is streamlit session_state.
    we create a messages inside it which store the role and the content based on the keys and their values this statement below is important.
    defination of memory object:
        if "messages" not in st.session_state:
        st.session_state.messages =[]
    storing data init:
        st.session_state.messages.append({"role":"user","content":prompt})
        st.session_state.messages.append({"role":"ai","content":result})
    printing it on fronend:
        for message in st.session_state.messages:
        st.chat_message(message["role"]).markdown(message["content"])

thats it project is simple you prompt the agent he see the database create task and show you the tasks,
once the task is created it alters it if you say him to do.
